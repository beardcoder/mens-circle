# Asset caching for Vite build output
# These files have content hashes in filenames, safe to cache forever

location ^~ /build/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    access_log off;
}
